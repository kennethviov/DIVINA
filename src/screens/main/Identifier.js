import { Text, View } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context';

export default function Identifier() {
    return (
      <SafeAreaView style={{ flex: 1 }}>
        <Text>Identifier Screen</Text>
      </SafeAreaView>
    );
}