import { Text, View } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context';

export default function DivePlans() {
    return (
      <SafeAreaView style={{ flex: 1 }}>
        <Text>Dive Plans Screen</Text>
      </SafeAreaView>
    );
}