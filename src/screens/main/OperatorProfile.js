import { Text, View } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context';

export default function OperatorProfile() {
    return (
      <SafeAreaView style={{ flex: 1 }}>
        <Text>Operator Profile Screen</Text>
      </SafeAreaView>
    );
}